# Frontend Environment Configuration

# API URL for production (Cloudflare Tunnel URL)
# Leave empty for development (uses proxy in package.json)
# REACT_APP_API_URL=https://your-tunnel-name.trycloudflare.com

# Example with custom domain:
# REACT_APP_API_URL=https://api.neighborhood-ai.yourdomain.com
