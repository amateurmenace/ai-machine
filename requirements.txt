# Core Framework
fastapi>=0.109.0,<0.110.0
uvicorn[standard]>=0.27.0,<0.28.0
python-multipart>=0.0.6
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0

# Database & Vector Store
# The default archive is SQLite, which is in the standard library. qdrant-client
# is only needed by deployments that already have an embedded index, and psycopg
# only by multi-community deployments; neither is imported unless selected.
qdrant-client>=1.7.0,<2.0.0
sentence-transformers>=2.3.1,<3.0.0
# Comes in with sentence-transformers anyway, listed because the SQLite store
# uses it directly: without numpy the vector scan falls back to pure Python and
# gets roughly twenty times slower on a large archive.
numpy>=1.24
# Optional. Install it if the archive passes a few hundred thousand passages and
# the vector scan starts to show; the store loads it automatically when present.
# sqlite-vec>=0.1.0

# Data Collection
youtube-transcript-api>=0.6.2
google-api-python-client>=2.116.0
beautifulsoup4>=4.12.3
requests>=2.31.0
pypdf>=4.0.1
python-docx>=1.1.0
playwright>=1.40.0
yt-dlp>=2024.3.10

# AI & LLM
ollama>=0.1.6
openai>=1.10.0
anthropic>=0.18.1

# Utilities
python-dotenv>=1.0.0
aiofiles>=23.2.1
httpx>=0.25.2,<0.26.0
apscheduler>=3.10.4
validators>=0.22.0

# Community AI layer (see COMMUNITY_AI_SCOPE.md)
# Hybrid retrieval's keyword half and the constitution loader are pure stdlib on
# purpose, so they keep working when the model stack is unavailable.
PyYAML>=6.0          # community.yaml / sources.yaml
cryptography>=42.0        # Ed25519 ratification signatures on the constitution ledger

# Google Cloud backends (see cloud/ and deploy/README.md). Deliberately not
# installed: each one is optional, is chosen by a single environment variable,
# and degrades to the local backend with an operator-facing message when it is
# absent. A community running on one machine should not have to install four
# Google packages to answer a question about a zoning bylaw.
# redis>=5.0                         # COMMUNITY_REDIS_URL, shared rate limits
# google-cloud-storage>=2.14         # COMMUNITY_STORAGE_BUCKET, uploaded PDFs
# google-cloud-secret-manager>=2.18  # sm:// references in config fields
# google-cloud-logging>=3.9          # COMMUNITY_CLOUD_LOGGING, request records
# psycopg[binary,pool]>=3.1          # COMMUNITY_DB_URL, the archive in PostgreSQL
